name: Building

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ main ]

jobs:
  Building_The_Package:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: List available Xcode versions
      run: ls /Applications | grep Xcode
    - name: Show current version of Xcode
      run: xcodebuild -version
    - name: Build macOS
      run: swift build -v
    - name: Build iOS
      run: xcodebuild build -scheme 'sRouting' -destination 'platform=iOS Simulator,OS=18.0,name=iPhone 16'
